<div class="max-w-lg mx-auto p-6 bg-gray-800 rounded-lg shadow-lg">
  <h1 class="text-xl font-bold text-white mb-4">Upload File</h1>
  <p class="text-gray-300 mb-4">
    You are uploading files to <strong><%= folderName %></strong> folder.
  </p>

  <form
    action="/upload/<%= folderId %>"
    method="post"
    enctype="multipart/form-data"
    class="space-y-4"
  >
    <div>
      <label for="uploadedFile" class="text-gray-300">Upload File:</label>
      <input
        type="file"
        id="uploadedFile"
        name="uploadedFile"
        multiple
        class="mt-1 block w-full px-4 py-2 bg-gray-700 text-white rounded-lg border border-gray-600 focus:ring-purple-400"
      />
    </div>
    <button
      type="submit"
      class="w-full px-5 py-2 bg-gradient-to-r from-purple-500 to-pink-500 text-white font-semibold rounded-lg shadow-md hover:from-purple-600 hover:to-pink-600 transition"
    >
      ⬆️ Add to Folder!
    </button>
  </form>
</div>
